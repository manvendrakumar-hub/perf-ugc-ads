# SECRETS — never commit
.secrets/
*.token.json
token.json
service_account.json
*client_secret*.json

# OS / cruft
.DS_Store
**/.DS_Store

# Python
__pycache__/
*.pyc
.venv/

# Node
node_modules/

# Large video outputs (these live in Drive; folder structure kept via .gitkeep)
*.mp4
*.mov

# Editor / agent local state
.claude/
*.lock

# Scratch / QA artifacts (debug clips, thumbnails)
**/_scratch/

# PII — real-person reference photos. NEVER commit (privacy). Live in Drive only.
Female face ref/

# Licensed music tracks — live in Drive only, not git (copyright)
Music/
*.mp3

# Third-party reference ads (research material; not our deliverables)
Video ref/

# QA thumbnail frames (scratch)
**/_frames/
